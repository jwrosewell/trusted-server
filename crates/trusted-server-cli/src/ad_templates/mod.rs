//! Pure, host-only ad-template CLI logic shared by the static `ts config
//! ad-templates ...` commands and the browser-backed `ts audit ad-templates
//! verify` command.

pub mod compare;
pub mod expected;
pub mod output;
